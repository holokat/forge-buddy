name: ForgeBuddy
options:
  bundleIdPrefix: com.forge
  deploymentTarget:
    iOS: "17.0"
settings:
  base:
    DEVELOPMENT_TEAM: RZM8C5A33N
    SWIFT_VERSION: 5.0
targets:
  ForgeBuddy:
    type: application
    platform: iOS
    sources:
      - ForgeBuddy
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.forge.buddy
        PRODUCT_NAME: Forge Buddy
        INFOPLIST_FILE: ForgeBuddy/Info.plist
        CODE_SIGN_STYLE: Automatic
        MARKETING_VERSION: 1.0
        CURRENT_PROJECT_VERSION: 1
